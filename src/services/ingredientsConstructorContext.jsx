import React from "react";

export const IngredientsConstructorContext = React.createContext(null);

export const TotalPrice = React.createContext(null);

export const OrderDetail = React.createContext(null);